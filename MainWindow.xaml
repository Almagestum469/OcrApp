<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="OcrApp.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:OcrApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="OcrApp">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid RowDefinitions="Auto,*" Padding="20">
        <StackPanel Orientation="Horizontal" Spacing="10">
            <Button x:Name="SelectCaptureItemButton" Content="选择捕获窗口" Click="SelectCaptureItemButton_Click" />
            <Button x:Name="RecognizeButton" x:FieldModifier="internal" Content="执行识别" Click="RecognizeButton_Click" IsEnabled="False" Margin="10,0,0,0"/>
        </StackPanel>
        <ListView x:Name="ResultListView" Grid.Row="1" Margin="0,20,0,0" Height="300" IsItemClickEnabled="True" ItemClick="ResultListView_ItemClick">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
